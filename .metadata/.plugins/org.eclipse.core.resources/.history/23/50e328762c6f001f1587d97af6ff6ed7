package 하성;

import java.util.List;

public interface Charge_ListDao {
	
	boolean insert(Charge chargeList);
	
	List<Charge> findAll();
	Charge findByManager_num(int chargeList);
	
	boolean update(Charge chargeList);
	
	boolean deleteByCharge_Num(int chargeList);
}
